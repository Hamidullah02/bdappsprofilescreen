import 'package:flutter/material.dart';

class videoCardWidget extends StatelessWidget {
  const videoCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
